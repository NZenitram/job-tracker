<h1><%= @category.title %></h1>

<ul>
  <% @category.jobs.each do |job| %>
    <li><%= link_to job.title, company_path(job.company) %></li>
  <% end %>
<%= link_to "Create New", new_category_url %>
<%= link_to "Categories Index", categories_path %>
